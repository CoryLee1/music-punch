sample.wav 已与项目一起在 Git 中跟踪，会推送到 GitHub；clone 后本文件即存在。

若你在本地替换了新的 sample.wav：
- 仍使用文件名 sample.wav
- 建议为 16-bit PCM 等浏览器易解码格式
- 替换后请 git add 并 commit，便于团队同步

验证：启动 dev 后打开 http://localhost:5173/sample.wav 应能播放 WAV，而非 HTML 页面。
